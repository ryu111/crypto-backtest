# 合約交易回測系統 - 專案配置

## 專案概述

BTC/ETH 永續合約 AI 自動化回測系統。

## 自動學習機制

### 回測後自動記錄

**每次執行回測後，必須自動判斷是否有新洞察並更新 `learning/insights.md`**

```
回測完成
    ↓
分析結果是否有新發現？
    │
    ├── 有新洞察 → 更新 learning/insights.md
    │   - 策略表現異常好/差的原因
    │   - 參數敏感度發現
    │   - 市場狀態影響
    │   - 過擬合警訊
    │   - 風險管理教訓
    │
    └── 無新發現 → 不需更新
```

### 應記錄的洞察類型

| 類型 | 觸發條件 | 記錄位置 |
|------|----------|----------|
| 策略洞察 | Sharpe > 2.0 或 < 0.5 | `## 策略類型洞察` |
| 標的特性 | 特定幣種表現異常 | `## 標的特性` |
| 風險發現 | MaxDD > 25% 或強平事件 | `## 風險管理洞察` |
| 過擬合警訊 | Monte Carlo 失敗率 > 30% | `## 過擬合教訓` |
| 參數發現 | 穩健性測試揭示敏感參數 | `## 策略類型洞察 > 對應策略` |

### 記錄格式範例

```markdown
#### [策略名稱]_[版本]
- **最佳參數**：param1=X, param2=Y
- **績效**：Sharpe X.XX, Return XX.X%, MaxDD XX.X%
- **洞察**：[具體發現]
- **日期**：YYYY-MM-DD
```

### 自動判斷邏輯

```python
def should_record_insight(result):
    """判斷是否值得記錄"""

    # 異常好的表現
    if result.sharpe > 2.0:
        return True, "exceptional_performance"

    # 異常差的表現（可能有 bug 或市場變化）
    if result.sharpe < 0.5 and result.expected_sharpe > 1.0:
        return True, "unexpected_poor_performance"

    # 過擬合警訊
    if result.overfit_probability > 0.3:
        return True, "overfit_warning"

    # 風險事件
    if result.max_drawdown > 0.25:
        return True, "risk_event"

    # 參數敏感度發現
    if result.robustness_variance > 0.5:
        return True, "parameter_sensitivity"

    return False, None
```

## Skills 系統

### 10 個領域 Skills

```
.claude/skills/
├── 資料管道/SKILL.md
├── 指標庫/SKILL.md
├── 策略開發/SKILL.md
├── 永續合約/SKILL.md
├── 參數優化/SKILL.md
├── 策略驗證/SKILL.md
├── 風險管理/SKILL.md
├── 學習系統/SKILL.md
├── 回測核心/SKILL.md
└── AI自動化/SKILL.md
```

### Skills 階層架構

```
┌─────────────────────────────────────────────────────────────┐
│                    AI 自動化 (協調層)                        │
│   統籌所有 Skills，決定何時調用哪個 Skill                    │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────┬──────────────┬──────────────┬───────────────┐
│   策略開發   │   參數優化    │   策略驗證    │   學習系統     │
│   (設計層)   │   (優化層)   │   (品質層)    │   (記錄層)    │
└──────────────┴──────────────┴──────────────┴───────────────┘
                              ↓
┌──────────────┬──────────────┬──────────────┬───────────────┐
│   回測核心   │   風險管理    │   永續合約    │   資料管道     │
│   (執行層)   │   (風控層)   │   (領域層)    │   (資料層)    │
└──────────────┴──────────────┴──────────────┴───────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      指標庫 (基礎層)                         │
│   技術指標計算，供所有策略使用                               │
└─────────────────────────────────────────────────────────────┘
```

### Skills 調用關係矩陣

| 調用者 ↓ / 被調用 → | 策略開發 | 回測核心 | 策略驗證 | 參數優化 | 風險管理 | 永續合約 | 學習系統 | 資料管道 | 指標庫 |
|-------------------|:-------:|:-------:|:-------:|:-------:|:-------:|:-------:|:-------:|:-------:|:-----:|
| **AI自動化** | ✓ | ✓ | ✓ | ✓ | - | - | ✓ | - | - |
| **策略開發** | - | ✓ | - | - | ✓ | ✓ | - | - | ✓ |
| **回測核心** | - | - | - | - | ✓ | ✓ | - | ✓ | - |
| **策略驗證** | - | ✓ | - | - | - | - | - | - | - |
| **參數優化** | - | ✓ | ✓ | - | - | - | - | - | - |
| **風險管理** | - | - | - | - | - | ✓ | - | - | - |

### 觸發關鍵字

| 關鍵字 | 載入 Skill | 使用場景 |
|--------|-----------|---------|
| 策略, strategy, 設計 | 策略開發 | 設計新策略、修改策略邏輯 |
| 回測, backtest, 測試 | 回測核心 | 執行回測、查看績效 |
| 驗證, validate, 檢驗 | 策略驗證 | 驗證策略品質、統計檢驗 |
| 優化, optimize, 參數 | 參數優化 | 參數調校、Walk-Forward |
| 風控, risk, 部位, 止損 | 風險管理 | 部位計算、止損策略 |
| 永續, perpetual, 資金費率 | 永續合約 | 合約特有知識、強平計算 |
| 學習, learning, 記錄 | 學習系統 | 實驗記錄、洞察查詢 |
| loop, 自動化, 循環 | AI自動化 | 啟動 AI Loop、自動化回測 |
| 資料, data, OHLCV | 資料管道 | 資料獲取、清理、驗證 |
| 指標, indicator, RSI, MA | 指標庫 | 技術指標選擇、計算方法 |

### 典型工作流調用鏈

```
用戶：「規劃一個 RSI 策略並執行 100 次 loop」
    ↓
AI 自動化啟動 Loop
    ↓
    ├─→ 策略開發: 選擇 RSI 策略類型
    │       ├─→ 指標庫: 獲取 RSI 計算方法
    │       ├─→ 風險管理: 設定止損止盈
    │       └─→ 永續合約: 槓桿/強平計算
    ↓
    ├─→ 參數優化: Bayesian 優化
    │       ├─→ 回測核心: 執行回測
    │       │       ├─→ 資料管道: 獲取 OHLCV
    │       │       ├─→ 永續合約: 資金費率
    │       │       └─→ 風險管理: 部位計算
    │       └─→ 策略驗證: 驗證結果
    ↓
    ├─→ 策略驗證: 5 階段驗證
    │       └─→ 回測核心: 多次回測
    ↓
    └─→ 學習系統: 記錄實驗
            └─→ Memory MCP: 存入洞察
```

## 關鍵檔案

| 檔案 | 用途 |
|------|------|
| `learning/insights.md` | 策略洞察彙整（自動更新） |
| `learning/experiments.json` | 實驗記錄（機器可讀） |
| `src/learning/recorder.py` | 實驗記錄器 |
| `src/learning/memory.py` | Memory MCP 整合 |
| `src/automation/loop.py` | AI 自動化循環 |

## 🧠 Memory MCP 整合

**insights.md 與 Memory MCP 雙向同步：**

```
洞察產生
    ↓
寫入 learning/insights.md（本地檔案）
    ↓
同時存到 Memory MCP（跨 session 查詢）
```

### 存入 Memory MCP 的時機

| 洞察類型 | 觸發條件 | Memory 標籤 |
|----------|----------|-------------|
| 策略成功 | Sharpe > 2.0 | `strategy,success,[策略名]` |
| 策略失敗 | Sharpe < 0.5 | `strategy,failure,[策略名]` |
| 過擬合 | MC 失敗率 > 30% | `overfit,[策略名]` |
| 風險事件 | MaxDD > 25% | `risk,[策略名]` |
| 參數發現 | 穩健性差異大 | `parameter,[策略名]` |

### Memory MCP 操作範例

```python
# 存入洞察（AI 執行）
store_memory(
    content="MA Cross 策略在 BTC 4h 表現優異，Sharpe 2.3",
    metadata={
        "tags": "strategy,success,ma_cross,btc",
        "type": "insight"
    }
)

# 查詢歷史（新 session 開始時）
retrieve_memory(query="ma_cross 策略 BTC")
search_by_tag(tags=["strategy", "btc"])
```

### 新 Session 開始時

**自動查詢相關經驗：**
```
1. 讀取當前任務的策略/標的
2. 查詢 Memory MCP 相關經驗
3. 整合到決策中
```

## 回測工作流

```
1. 選擇策略（80% exploit / 20% explore）
2. 生成參數（Bayesian 優化或隨機）
3. 執行 5 階段驗證
4. 記錄結果到 experiments.json
5. 【新增】判斷是否更新 insights.md
6. 【新增】同步到 Memory MCP
7. 更新策略評級
8. 繼續下一輪
```
